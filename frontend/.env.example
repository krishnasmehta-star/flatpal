# Leave empty when the API is served on the same origin (e.g. Vite dev proxy).
# In production set this to the Render backend URL, no trailing slash, e.g. https://flatpal-api.onrender.com
VITE_BACKEND_URL=
