# MongoDB Atlas connection string (free M0 cluster). If unset, the API uses a local JSON file (dev only).
MONGO_URL=
DB_NAME=flatpal
# Comma-separated list of allowed frontend origins, e.g. https://flatpal.vercel.app,https://flatpal.in
CORS_ORIGINS=*
